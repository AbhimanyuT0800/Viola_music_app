import 'package:flutter/material.dart';

class SongsInList extends StatelessWidget {
  const SongsInList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('song'),
      ),
    );
  }
}
